# Archivo de ejemplo para configuración de Spring Boot
# Copia este archivo como application.properties y completa los valores según tu entorno

spring.application.name=Inventory System
spring.datasource.url=jdbc:postgresql://localhost:5432/tu_basededatos
spring.datasource.username=tu_usuario
spring.datasource.password=tu_contraseña

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

security:
    jwt:
        secret=pon_aqui_tu_jwt_secret
#spring.security.debug=false
#jwt.secret=miClaveSecretaParaJWT
#jwt.expiration=3600000

